# coding: utf-8

import setuptools


NAME = 'jason'
VERSION = '0.0.1'
REQUIRES = [

]


setuptools.setup(
    name=NAME,
    version=VERSION,
    install_requires=REQUIRES,
    packages=setuptools.find_packages()
)
